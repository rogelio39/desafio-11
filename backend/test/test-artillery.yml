config:
  target: 'http://localhost:4000/operacionCompleja'
  phases:
    - duration: 60
      arrivalRate: 5

scenarios:
  - flow:
    - get:
        url: '/'
        headers:
          authorization: 'aqui copiamos un token de jwt'